# Copy to .env.local and fill in your Supabase publishable (anon) key.
# Dashboard: Project Settings → API → publishable key (starts with sb_publishable_ or eyJ...)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-publishable-anon-key

# Canonical site URL for sitemap, robots, and metadata (no trailing slash).
# Example: https://www.checkmyfood.net
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Google Analytics 4 Measurement ID (G-XXXXXXXXXX). Leave unset locally to avoid polluting production data.
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Optional site password gate. Unset, off, or false = public site.
# Set to any other string to require that password at /login.
# SITE_PASSWORD=your-password
